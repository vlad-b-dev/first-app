import React, { useRef, useEffect } from "react";
import * as THREE from "three";

const MainPageBacgroundAnimation = React.memo(({ width = 50, height = 50 }) => {
  const mountRef = useRef(null);

  return <div />;
});

export default MainPageBacgroundAnimation;
